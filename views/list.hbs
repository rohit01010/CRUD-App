<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>CURD APP | INSERT USER</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">   
    <style>
        @import url('https://fonts.googleapis.com/css?family=Montserrat:100,200,300,400,500,600,700&display=swap');
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Montserrat', sans-serif;
        }
        body{
            background: #fec107;
            width: 100%;
            height: 100vh;
            padding: 50px;
            display:flex;
            justify-content: center;
        }
        .wrapper{
            width: 100%;
            height: 100%;
            overflow-y: scroll;
            background: #fff;;
            box-shadow: 1px 1px 2px rgba(0,0,0,0.125);
            padding: 30px;
        }
        .wrapper .title{
            width: 100%;
            margin: 20px 0;
            font-size: 2.5rem;
            font-weight: 700;
            display: flex;
            justify-content: space-between; 
        }
        .wrapper .btn{
            width: fit-content;
            height: fit-content;
            padding: 7px 15px;
            align-self: flex-end;
            font-size: 15px; 
            font-weight: 600;
            border: 0px;
            background:  #fec107;
            color: #fff;
            cursor: pointer;
            text-decoration: none;
            border-radius: 3px;
            outline: none;
        }
        .wrapper .btn:hover{
            background: #ff6c03
        }
        .wrapper td{
            font-weight: 500;
        }
        .wrapper tr td:last-child{
            display: flex;
            justify-content: space-evenly;
        }
        .wrapper tr td:last-child a{
            margin: 0 7px;
        }
        @media (max-width: 900px) {
            body{
                padding: 20px;
            }
            .wrapper{
                padding: 20px;
            }
        }
    </style>
</head>
<body>

<div class="wrapper">
    <h3 class="title">
        Users
        <a class="btn btn-primary" href="/user/insert"><i class="fa fa-plus"></i> Create New</a>
    </h3>
    <div class="table-responsive">
        <table class="table table-hover table-bordered table-striped">
            <thead>
                <tr>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Email</th>
                    <th>Roll Number</th>
                    <th>Phone</th>
                    <th>Pin Number</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {{#each list}}
                <tr>
                    <td>{{this.firstname}}</td>
                    <td>{{this.lastname}}</td>
                    <td>{{this.email}}</td>
                    <td>{{this.rollnumber}}</td>
                    <td>{{this.phone}}</td>
                    <td>{{this.pinNumber}}</td>
                    <td>
                        <a class="btn" title="Edit" style="padding: 5px 10px;" href="/user/update/{{this._id}}"><i class="fa fa-pencil fa-lg" aria-hidden="true"></i></a>
                        <a class="btn" title="Delete" style="padding: 5px 10px;" href="/user/delete/{{this._id}}" onclick="return confirm('Are you sure to delete this record ?');"><i class="fa fa-trash fa-lg" aria-hidden="true"></i></a>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>   
</div>
</body>
</html>
    