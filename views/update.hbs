<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>CURD APP | INSERT USER</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" >
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">    
    <style>
        @import url('https://fonts.googleapis.com/css?family=Montserrat:500,700&display=swap');
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Montserrat', sans-serif;
        }
        body{
            background: #fec107;
            width: 100%;
            height: 100vh;
            display:flex;
            justify-content: center;
            align-items:center;
        }
        input::-webkit-outer-spin-button,
        input::-webkit-inner-spin-button {
        -webkit-appearance: none;
        margin: 0;
        }
        input[type=number] {
        -moz-appearance: textfield;
        }
        .wrapper{
            max-width: 700px;
            width: 100%;
            background: #fff;
            box-shadow: 1px 1px 2px rgba(0,0,0,0.125);
            padding: 30px;
        }
        .wrapper .title{
            font-size: 30px;
            font-weight: 700;
            margin-bottom: 25px;
            text-transform: uppercase;
            text-align: center;
        }
        .wrapper .form{
            width: 100%;
        }
        .wrapper .form .inputfield{
            margin-bottom: 15px;
            display: flex;
            align-items: center;
        }
        .wrapper .form .inputfield label{
            width: 200px;
            color: #757575;
            margin-right: 10px;
            font-size: 14px;
            font-weight: 700;
        }
        .wrapper .form .inputfield .input{
            width: 100%;
            outline: none;
            border: 1px solid #d5dbd9;
            font-size: 15px;
            padding: 8px 10px;
            border-radius: 3px;
            transition: all 0.3s ease;
        }
        .wrapper .form .inputfield .input:focus{
            border: 1px solid #fec107;
        }
        .wrapper .form .inputfield .input::placeholder{
            color: #0007;
        }
        .wrapper .form .inputfield p{
            font-size: 14px;
            color: #757575;
        }
        .wrapper .form .inputfield .btn{
            width: fit-content;
            padding: 8px 10px;
            margin-right: 20px;
            font-size: 15px; 
            border: 0px;
            background: #ff9900; 
            color: #fff;
            cursor: pointer;
            text-decoration: none;
            border-radius: 3px;
            outline: none;
        }
        .wrapper .form .inputfield .btn:hover{
            background: #ff5e00;
        }
        .wrapper .form .inputfield:last-child{
            margin-bottom: 0;
        }
        @media (max-width:420px) {
            .wrapper .form .inputfield{
                flex-direction: column;
                align-items: flex-start;
            }
            .wrapper .form .inputfield:last-child{
                flex-direction: row;
            }
            .wrapper .form .inputfield label{
                margin-bottom: 5px;
            }
            .wrapper .form .inputfield.terms{
                flex-direction: row;
            }
        }
    </style>
</head>
<body>

<div class="wrapper">
    <h3 class="title">{{viewTitle}}</h3>
    <form class="form" action="/user/update" method="POST" autocomplete="off">
        <input type="hidden" name="_id" value="{{user._id}}">
        <div class="inputfield">
            <label>First Name</label>
            <input type="text" class="input" name="firstname" placeholder="First Name"required="true" value="{{user.firstname}}">
        </div>
        <div class="inputfield">
            <label>Last Name</label>
            <input type="text" class="input" name="lastname" placeholder="Last Name" required="true" value="{{user.lastname}}">
        </div>
        <div class="inputfield">
            <label>Email</label>
            <input type="email" class="input" name="email" placeholder="Email" required="true" value="{{user.email}}">
        </div>
        <div class="inputfield">
            <label>Roll Number</label>
            <input type="text" class="input" name="rollnumber" placeholder="Roll Number" required="true" value="{{user.rollnumber}}">
        </div>
        <div class="inputfield">
            <label>Phone Number</label>
            <input type="number" class="input" name="phone" placeholder="Phone Number" max="9999999999" min="1000000000"  required="true" value="{{user.phone}}">
        </div>
        <div class="inputfield">
            <label>Pin Number</label>
            <input type="number" class="input" name="pinNumber" placeholder="Pin Number" required="true" value="{{user.pinNumber}}">
        </div>
        <div class="inputfield">
            <button type="submit" class="btn"><i class="fa fa-database"></i> Submit</button>
            <a class="btn" href="/user/list"><i class="fa fa-list-alt"></i> View All</a>
        </div>
    </form>
</div>

</body>
</html>
    